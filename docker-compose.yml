services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: site-roco:latest
    container_name: site-roco
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL:-http://localhost:3000}
    env_file:
      - .env
    restart: unless-stopped
