# Active Context — ROCO

## Sessão atual (2026-08-25)
Implementação da feature: footer dinâmico + contato do site + menu "Ligamos pra você".
Tudo entregue e commitado em `feat/porta-mais-site`. Build limpo, 934 testes.
68 commits à frente da `main`.

## Estado do repositório
- Branch: `feat/porta-mais-site`
- remote: `origin/feat/porta-mais-site` (pushado)
- `.env.local` de produção: `PRODUCTION_DATABASE_URL` configurado

## Pending
- merge `feat/porta-mais-site` → `main` + push
- seed em produção: `npm run db:seed` com `DATABASE_URL` de produção
- Provisionar RD Station API Key + Resend API Key
- Publicar o site em produção (main está 68 commits atrás)
